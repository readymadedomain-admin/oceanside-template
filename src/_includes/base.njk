<!DOCTYPE html>
<html lang="en" data-theme="{{ theme.colorway }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ business.meta.businessName }} | {{ business.meta.city }}</title>
        <link rel="stylesheet" href="./css/style.css">
    </head>
    <body id="top">
        {% include "header.njk" %}
        <main>
            {{ content | safe }}
        </main>
        {% include "footer.njk" %}
    </body>
</html>